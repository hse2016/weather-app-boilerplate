## Example weather app boilerplate

После `git clone`:

`npm install`
`npm run server`

Browserify:

`npm run build:once`
`npm run build:watch`

После правки index.pug запускаем `npm run build:html` для сборки странички

